{
  "name": "eticket-watcher",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "postinstall": "cross-env PLAYWRIGHT_BROWSERS_PATH=./playwright-browsers npx playwright install chromium-headless-shell"
  },
  "dependencies": {
    "express": "^4.21.0",
    "node-fetch": "^3.3.2",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}